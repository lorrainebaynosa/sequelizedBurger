<h1>Eat-Da-Burger!</h1>

<h2>Burgers Waiting to be Devoured</h2>

<ul>
  {{#each burgers}}
    {{#unless devoured}}
      {{> burgers/burger-block devoured=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Burgers that have been Devoured</h2>

<ul>
  {{#each burgers}}
    {{#if devoured}}
      {{> burgers/burger-block devoured=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="bu">Enter a burger you want to eat and click the "Add Burger" button</label>
    <input type="text" id="bu" name="name">
  </div>

  {{!-- where <form class="create-form"> refers to --}}

  {{!-- <div class="form-group">
    <label for="devoured">devoured?</label><br>
    <input type="radio" name="devoured" value="1" checked> devoured!<br>
    <input type="radio" name="devoured" value="0"> Awake
  </div> --}}

  <button type="submit">Add Burger</button>
</form>
{{!-- To use a variable with handlebars, use {{}} (1:04:00, https://www.youtube.com/watch?v=L72fhGm1tfE--}}
{{!-- alternatively, you can write this.lunch --}}